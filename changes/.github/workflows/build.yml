name: Build Telegram APK

on:
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      build_variant:
        description: 'نوع خروجی (Build Variant)'
        required: true
        default: 'Standalone'
        type: choice
        options:
          - 'Standalone'
          - 'Release'
          - 'Debug'
          - 'All'
      target_abi:
        description: 'معماری پردازنده (ABI) - برای بیلد فوق سریع arm64-v8a را انتخاب کنید'
        required: false
        default: 'arm64-v8a'
        type: choice
        options:
          - 'arm64-v8a'
          - 'All'
          - 'armeabi-v7a'

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Ensure Submodules are synced
        run: |
          git submodule sync --recursive
          git submodule update --init --recursive --depth 1

      - name: Expand Swap Space
        run: |
          sudo swapoff -a || true
          sudo rm -f /swapfile || true
          sudo fallocate -l 6G /mnt/swapfile || sudo dd if=/dev/zero of=/mnt/swapfile bs=1M count=6144
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          free -h

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ${{ runner.os }}-ccache-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-
          max-size: 3G

      - name: Install Android SDK, NDK and CMake
        run: |
          sudo chown -R $USER:$USER $ANDROID_HOME
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
            "platforms;android-36" \
            "build-tools;36.0.0" \
            "cmake;3.22.1" \
            "ndk;27.2.12479018"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/27.2.12479018" >> $GITHUB_ENV

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Ensure Qorvhex Keystore exists
        run: |
          mkdir -p TMessagesProj/config
          if [ ! -f "TMessagesProj/config/qorvhex.keystore" ]; then
            echo "Generating dedicated Qorvhex release keystore..."
            keytool -genkeypair -v \
              -keystore "TMessagesProj/config/qorvhex.keystore" \
              -alias "qorvhex" \
              -keyalg RSA \
              -keysize 2048 \
              -validity 10000 \
              -dname "CN=QorvhexGram, OU=Qorvhex, O=Qorvhex, L=Tehran, ST=Tehran, C=IR" \
              -storepass "qorvhexgram123" \
              -keypass "qorvhexgram123"
          fi

      - name: Build APK
        env:
          BUILD_VARIANT: ${{ github.event.inputs.build_variant || 'Standalone' }}
          TARGET_ABI: ${{ github.event.inputs.target_abi || 'arm64-v8a' }}
        run: |
          GRADLE_ARGS="--stacktrace --info --no-daemon -Dorg.gradle.jvmargs=-Xmx4g -PtargetAbi=$TARGET_ABI"
          echo "Building variant: $BUILD_VARIANT with target ABI: $TARGET_ABI"
          if [ "$BUILD_VARIANT" = "Standalone" ]; then
            ./gradlew :TMessagesProj_AppStandalone:assembleAfatStandalone $GRADLE_ARGS
          elif [ "$BUILD_VARIANT" = "Release" ]; then
            ./gradlew :TMessagesProj_App:assembleAfatRelease $GRADLE_ARGS
          elif [ "$BUILD_VARIANT" = "Debug" ]; then
            ./gradlew :TMessagesProj_App:assembleAfatDebug $GRADLE_ARGS
          elif [ "$BUILD_VARIANT" = "All" ]; then
            ./gradlew :TMessagesProj_AppStandalone:assembleAfatStandalone :TMessagesProj_App:assembleAfatRelease $GRADLE_ARGS
          fi

      - name: Dump CMake and Ninja logs on failure
        if: failure()
        run: |
          echo "=== CMake and Ninja Logs ==="
          find TMessagesProj/.cxx -type f \( -name "*.log" -o -name "*.txt" \) -exec echo "--- {} ---" \; -exec tail -n 100 {} \; || true

      - name: Collect APKs
        run: |
          mkdir -p output_apks
          find . -name "*.apk" -exec cp {} output_apks/ \;
          echo "Generated APKs:"
          ls -lh output_apks/

      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: QorvhexGram-APKs
          path: output_apks/*.apk
          retention-days: 14

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: output_apks/*.apk
          draft: false
          prerelease: false
          generate_release_notes: true
